# These credentials are authoritative on every startup.
# The application will not start if the password is missing or blank.
SANDBOX_MCP_WEB_USERNAME=admin
SANDBOX_MCP_WEB_PASSWORD='replace-with-a-strong-password'

# Port published on host loopback.
SANDBOX_MCP_PORT=8080
